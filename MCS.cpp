# include "stdio.h"

int main(){
    int MCS(int a[], int s, int t);
    int array[10] = {4, -2, 6, 1, 0, -5,-1, 7,-3, 5};
    m = floor((10 + 1)/2);
    left = MCS(a, 0, m);
    right = MCS(a, m, 10);
    middle = combine()
    return 0;
}

int MCS(int a[], int s, int t)
{

}